# Corpus taxonomy and source-policy defaults

Date: 2026-06-06
Type: docs
Docs: `docs/architecture`, `docs/operations`, `docs/research`, `docs/roadmaps`

## Summary

Completed Workstream 1 pass 1 for the public-launch corpus plan.

## Changes

- Added `docs/architecture/corpus-taxonomy.md` as the durable source-class and topic-cluster
  contract for broad GPT-era AI-history coverage.
- Defined conservative per-class source-policy defaults and public display rules without claiming
  unimplemented adapters or unverified redistribution rights.
- Aligned the baseline seeding report with the locked taxonomy, seed-vocabulary conclusion,
  first-proof query set, and full-corpus acceptance query shape.
- Updated the active roadmap to mark Workstream 1 pass 1 complete and defer implementation to
  downstream adapter/backfill/query-quality workstreams.

## Verification

Docs-only verification: read back changed Markdown and `git diff --check`.
