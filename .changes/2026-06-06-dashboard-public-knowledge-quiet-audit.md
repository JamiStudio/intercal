Workstream 5 quiet audit:

- Restricted public citation chips to `http` and `https` outbound links, with invalid or non-web
  citation URLs falling back to source-record metadata instead of link rendering.
- Added an explicit unavailable evidence state for topic timeline claims that return without
  citations.
