---
type: changed
area: operations
---

Added SQL-owned observability views and `pnpm ops:health` so operators can inspect source health,
failed jobs, pipeline quality, REST/MCP latency, freshness, and provider consumption against the
resource budget without fake zero-filled provider telemetry.
