Workstream 4 seeded corpus-quality verification now matches the live schema and query contract:
rollback proof claims include extraction provenance, the delta proof checks `summary.citations`,
and the true GPT-4 Turbo 128k point-in-time claim verifies as supported while the wrong 1M claim
remains adversarially contradicted.
